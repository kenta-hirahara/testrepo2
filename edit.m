            dx = app.dx;
            dy = app.dy;
            dt = app.dt;
            nx = app.nx;
            ny = app.ny;
            ntime = app.ntime;
            wc = app.wc;
            theta = app.theta;
            phi = app.phi;
                     cv = app.cv;
                     ajamp = app.ajamp;
            omega = app.omega;